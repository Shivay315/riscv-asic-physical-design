# Scripts

No complete Cadence Genus or Innovus TCL implementation scripts were present in the supplied RISC-V archive.

The repository therefore documents the flow and preserves the supplied RTL, but does not invent executable implementation scripts.

To reproduce the flow, the missing scripts would need to be recreated against the authorized SCL/PDK environment.
